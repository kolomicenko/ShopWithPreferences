/**
 * @file
 * Script for catalog (catalog.php).
 * Creates <a href="../js_docs_out/PageNumbers.html">PageNumbers</a>
 */

$(document).ready(function(){
    new PageNumbers('Top');
    new PageNumbers('Bottom');
});
